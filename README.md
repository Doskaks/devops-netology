=======
В терраформовском гитигноре будут игнорироваться файлы следующего вида:  
  - все файлы состояния Terraform заканчивающиеся на .tfstate и начинающиеся на .tfstate;
  - файлы логов аварийных завершений crash.log в корне и вида crash.любые-символы.log;
  - все файлы переменных .tfvars и .tfvars.json;
  - файлы переопределения override.tf в корне, override.tf.json в корне, любые файлы заканчивающиеся на _override.tf и на _override.tf.json;
  - файл блокировки состояния.terraform.tfstate.lock.info;
  - файлы конфигурации .terraformrc и terraform.rc

Новая строка в ветке fix

Привет PyChrm

PyCharm (1)

PyCharm (2)

PyCharm (3)

## Задание 4

![PyCharm]()